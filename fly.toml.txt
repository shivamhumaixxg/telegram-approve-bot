# Fly.io Configuration for Telegram Bot
app = "telegram-approve-bot"
primary_region = "sin"  # Singapore (change if needed)

[build]
  dockerfile = "Dockerfile"

[env]
  # BOT_TOKEN secret se aayega, yahan mat daalo

[[vm]]
  size = "shared-cpu-1x"
  memory = "256mb"
